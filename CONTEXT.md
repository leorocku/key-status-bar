# KeyStatusBar

键盘信号穿过 30 米设备信号增强器后的桌面纠错语境：展示主机当前认为哪些键处于按下状态，并自动解除卡住的键。

## 硬件与故障

**设备信号增强器**:
发射器 + 接收器对，经网络双绞线把键盘/鼠标/显示器信号延伸约 30 米。已知会丢失或错误解析信号。
_Avoid_: KVM 延长器、信号放大器

**信号丢失**:
键盘产生的按下或释放事件没有到达主机。最典型：释放信号丢失，键在主机侧停留在按下状态。
_Avoid_: 丢包

**信号解析错误**:
主机收到的事件与键盘实际发出的不一致（如发出"按下 C、释放 C"，主机收到"按下 B、释放 C"）。
_Avoid_: 乱码键

**卡住的键**:
主机认为仍被按下、但键盘物理侧已经释放的键。是信号丢失/信号解析错误的直接后果，表现为自动重复不止或修饰键全局生效。
_Avoid_: 长按（用户物理按住不是卡住）

**自动重复**:
主机认为某键处于按下状态期间，系统持续合成该键输入的现象。卡住的键持续打字的原因。
_Avoid_: typematic、连击

## 按键分类

**修饰键**:
Shift、Ctrl、Alt、Win（Windows 上对应 "command"）。卡住的修饰键会污染其后所有输入。
_Avoid_: 功能键

**触发键**:
修饰键之外的所有按键。

## 状态条显示

**按键块**:
状态条上代表"主机当前认为处于按下状态的一个键"的块。收到释放事件（用户的物理释放或强制释放）后立即消失；同一按键同时最多只有一个按键块。
_Avoid_: 活块

**幽灵块**:
强制释放后留在状态条上高亮闪烁的块，闪烁时长到期后自行移除。它只记录"刚发生过一次强制释放"，不代表主机当前认为该键处于按下状态。
_Avoid_: 残影、闪烁块

## 自动纠错

**重复延迟**:
从系统收到按键按下到开始第一次自动重复之间的间隔。优先从系统读取，读不到时用默认值。
_Avoid_: 初始延迟（系统 API 叫 InitialDelay，但本项目统一用本词）

**自动释放延迟**:
一个键在被强制释放前允许保持按下状态的最长时间。值为重复延迟乘以释放乘数。

**按键按下计时**:
每个键的一次性按下时长计时。仅当计时为 0（键处于释放状态）时的按下事件才启动计时；计时运行期间的重复按下事件不刷新计时。被强制释放或收到真实释放信号后归零。

**强制释放**:
让主机把卡住的键恢复为释放状态的纠错动作。适用于所有按键（修饰键、触发键、切换键一律统一），无豁免键。
_Avoid_: 释放（主体不是用户时必须说"强制释放"，与用户的物理释放区分）

**强制释放循环**:
一个键被强制释放后又收到新的按下事件，按键按下计时重新启动并再次触发强制释放，如此反复的现象。状态条上表现为该键的按键块反复出现又消失、幽灵块不断堆积并依次到期消失。
_Avoid_: 振荡、振荡器

**修饰键连击续期**:
当一个修饰键发生全新按下（按下前计时器为 0）时，重置所有当前被按下的修饰键的按键按下计时，使其从当前时刻重新计时。目的是保护用户进行中的修饰键组合不被提前强制释放。仅由全新按下触发，自动重复不触发。

**释放闪烁**:
强制释放发生时状态条上的视觉提示：生成一个幽灵块高亮闪烁，指定时长后移除。闪烁时长由用户调整，范围 1 到 5 秒。用于让用户区分"卡键被修好"与"按着的键被误放"。

**启动清理**:
应用启动时执行的纠错动作：先查询全部按键状态，只对其中确实处于按下状态的键执行强制释放，并将全部按键按下计时归零。存量卡住的键立即解除，自动纠错从干净状态开始。

**自动纠错开关**:
用户控制自动纠错启停的开关，位于托盘菜单，默认开启，状态持久化。关闭时应用退回纯显示形态：只展示按键状态，不做计时与强制释放。

**释放乘数**:
作用于重复延迟、得出自动释放延迟的系数。由用户调整，允许范围 0.1 到 1。
_Avoid_: 比例、倍率

**自动纠错**:
监测按键按下计时并对卡住的键执行强制释放的机制。
