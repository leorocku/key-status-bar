# KeyStatusBar

键盘信号穿过 30 米设备信号增强器后的桌面诊断语境：展示主机当前认为哪些键处于按下状态，供用户识别卡住的键并手动补一次按下+释放来解除。

## 硬件与故障

**设备信号增强器**:
发射器 + 接收器对，经网络双绞线把键盘/鼠标/显示器信号延伸约 30 米。已知会丢失或错误解析信号。
_Avoid_: KVM 延长器、信号放大器

**信号丢失**:
键盘产生的按下或释放事件没有到达主机。最典型：释放信号丢失，键在主机侧停留在按下状态。
_Avoid_: 丢包

**信号解析错误**:
主机收到的事件与键盘实际发出的不一致（如发出"按下 C、释放 C"，主机收到"按下 B、释放 C"）。
_Avoid_: 乱码键

**卡住的键**:
主机认为仍被按下、但键盘物理侧已经释放的键。是信号丢失/信号解析错误的直接后果，表现为自动重复不止或修饰键全局生效。
_Avoid_: 长按（用户物理按住不是卡住）

**自动重复**:
主机认为某键处于按下状态期间，系统持续合成该键输入的现象。卡住的键持续打字的原因。
_Avoid_: typematic、连击

**幻影按下事件**:
用户没有按键、但链路侧（增强器或键盘固件）重放卡住状态而持续到达主机的按下事件。与真实按下的区别只在节奏：固定间隔心跳（实测约 500ms）且永远不伴随释放事件。应用无法在主机侧将其与真实按下区分，故不做过滤。

## 按键分类

**修饰键**:
Shift、Ctrl、Alt、Win（Windows 上对应 "command"）。卡住的修饰键会污染其后所有输入。
_Avoid_: 功能键

**触发键**:
修饰键之外的所有按键。

## 状态条显示

**按键块**:
状态条上代表"主机当前认为处于按下状态的一个键"的块。按下事件到达时出现，释放事件到达时消失；同一按键同时最多只有一个按键块。
_Avoid_: 活块

**重复闪烁**:
主机侧自动重复进行中（编辑器连吐字符）时，该键的按键块高亮闪烁的视觉提示。进入条件为连续 2 个重复 down（免疫信号解析错误的单个杂散 down）；键块随释放事件消失时闪烁同时消失，无独立生命周期。卡住的键在重复延迟后同样进入重复闪烁，故"手未碰键盘却有键块在闪"即卡键信号。
_Avoid_: 自动纠错闪烁、幽灵块
